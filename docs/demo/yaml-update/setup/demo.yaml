data:
  description: |
    This is to demonstrate how Importer can manage your YAML files.
    ...

  # == imptr: metadata / begin from: ./snippets.yaml#[metadata-only] indent: absolute 2 ==
  anything: here
  would: be
  purged: by Importer
  # == imptr: metadata / end ==

  some-data:
    description: |
      any data outside of Importer Markers are unaffected.
